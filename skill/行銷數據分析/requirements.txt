# 行銷數據分析 Skill — 主環境（Python 3.13 或 3.14）
#
#     pip install -r requirements.txt
#
# ⚠ 因果推論（dowhy / EconML / causalml / meridian）不在這裡。
#   Python 3.14 裝 dowhy 會靜默降版到 2022 年的 0.8 版且不警告。
#   那幾個套件請用 requirements-causal.txt 裝在 Python 3.12 次環境。

# ── 倉儲與資料處理 ───────────────────────────────────────
duckdb>=1.5
pyarrow>=17
polars>=1.20
pandas>=2.2
numpy>=2.0
openpyxl>=3.1
pyyaml>=6.0

# ── 資料品質 ─────────────────────────────────────────────
pandera>=0.22

# ── 統計（取代 R 那一套）─────────────────────────────────
scipy>=1.14
statsmodels>=0.14.5        # ols / glm / anova_lm / Cook's D / VIF / 巢狀 F
pingouin>=0.5.5            # 事後檢定、效果量
scikit-posthocs>=0.9       # 無母數事後檢定

# ── 機器學習與分群 ───────────────────────────────────────
scikit-learn>=1.5          # 1.3 起內建 HDBSCAN，不需另裝 hdbscan
xgboost>=2.1
lightgbm>=4.5
catboost>=1.2              # 高基數類別特徵最省事
shap>=0.46                 # 不要用 tree importance，對高基數欄位有偏

# ── 行銷專用模型 ─────────────────────────────────────────
mlxtend>=0.23              # 購物籃 apriori / fpgrowth
factor_analyzer>=0.5       # 因素分析，取代 SPSS
lifelines>=0.29            # 存活分析（注意：同作者的 lifetimes 已 archived）
scikit-survival>=0.23
statsforecast>=1.7
pymc-marketing>=0.10       # CLV (BG/NBD + Gamma-Gamma) 與 MMM
tea-tasting>=0.6           # A/B 測試、CUPED

# ── 文本 ─────────────────────────────────────────────────
jieba>=0.42                # 中文斷詞（繁中建議另裝 ckip-transformers）

# ── 視覺化與交付 ─────────────────────────────────────────
altair>=5.5
vl-convert-python>=1.7     # Altair → PNG/PDF，不需 node
matplotlib>=3.9
seaborn>=0.13
great-tables>=0.14
jinja2>=3.1                # 單檔 HTML 報告
xlsxwriter>=3.2
python-pptx>=1.0
marimo>=0.10               # 儀表板 L2/L3（L1 用 Altair 即可）

# ── 開發 ─────────────────────────────────────────────────
pytest>=8.0
